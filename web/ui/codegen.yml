schema: ../../graphql/schema.graphql
documents:
  - src/**/*.svelte
  - src/lib/operations/**/*.graphql
ignoreNoDocuments: true
generates:
  ./src/gql/index.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      useTypeImports: true
