query Inventory($input: InventoryInput!) {
     inventory(input: $input) {
         categories,
         games {
             id,
             title,
             imageUrl,
             category,
             price,
         }
     }
}